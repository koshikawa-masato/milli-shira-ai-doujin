# C109 申込の記録

## 2026-08-21 申込完了（Grok を使用）

- Circle.ms の申込フォームの「頒布物概要」欄の文面を **Grok** に書かせて提出した
- 共有リンク: https://grok.com/share/bGVnYWN5_0bdb239d-56c4-4b7e-90b9-0ad64c17b19c
- 共有ページのタイトル: 「AIでコミケC109申し込み成功」
- Grok への最初の依頼（フォームの指示文をそのまま渡している）:

  > 準備会（配置担当者）に向けた頒布物の概要、傾向を書いてください。参加者へのアピールはサークルカット等へ（最大１６０字まで・改行無効）

- Grok の回答本文（実際に提出した 160 字）は共有ページがログイン必須のため Claude 側からは取得できず。**提出文は下に貼って残す**（未記入）

### 提出した頒布物概要（160 字以内・改行なし）

```
（ここに提出文を貼る）
```

## 使った AI と役割分担（記録）

| 工程 | 使った AI / ツール |
|---|---|
| サークル名・本の位置づけ・章構成 | Claude Code（このリポジトリの `log/`） |
| 頒布物概要（160 字） | Grok |
| サークルカットの絵 | Krea 2 Turbo + 自作 LoRA `kuropanda_v1`（WSL2 / RTX 4060 Ti） |
| サークルカットの組版・グレースケール化 | Claude Code（`application/circle-cut/make_circlecut.py`） |
| 画像の確認・指示（iPhone） | 自作ギャラリー（Pi5 / Tailscale） |

## 方針メモ

- このリポジトリの内容はすべて「制作記録」であり、そのまま本の執筆素材になる。**PII（メールアドレス・電話・住所・トークン類）だけ入れない**。tailnet 内のホスト名・IP は可
