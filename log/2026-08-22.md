# 2026-08-22

## やったこと

### LoRA v2 — 合言葉が効いた
- 基準をサークルカットの子に更新。v1 LoRA×0.7 で 30 枚生成 → 耳ピンク 6 枚を除外、24 枚に黒目を自動描画・耳の白化（緩めすぎて肌まで白くした 6 枚は元からやり直し）
- 新ルール「ポケットの上端は紐の先端より下」（胸まで上がっていたら NG）。v2 素材 25 枚中 8 枚が該当 → 記録して v3 で除外
- `kuropanda_v2`: トリガー `krpn`、キャプションは `krpn, 1girl, ポーズ文` のみ、768 / 2400 step、6 時間 33 分
- **結果: `krpn` だけで衣装一式が出る**。800〜2400 で安定、採用候補 1200 / 2000。v1 の失敗原因（`panda` に負ける）を実証
- 途中、Pi5 の MineMoney が Tailscale :8452 を奪って Krea2 Gallery が外から見えなくなった → MineMoney を :8453 へ、Krea2 を :8452 に復旧

### ギャラリー
- 系統図を「系統一覧（フォルダのカード）→ ツリー → フォーカス」の 3 段に。iPhone のピンチズーム、PC のドラッグ・⌘ホイール、ホバー表示の廃止
- 生成ジョブに LoRA 倍率、学習ジョブにデータセット指定

### 判定ボットの検討
- Ollama Cloud は推論専用（学習・画像生成は不可）→ 学習時間の短縮には無関係。VLM 判定の候補として Claude API / Gemini / OpenRouter(Qwen3-VL) / ローカル を比較
- Mac（M5 32GB）で gemma4 を測定: ポケット判定 76%、黒目なし・耳ピンクの検出 0/4 → 不採用、削除
- Qwen3-VL 32B: 32K コンテキストだと 29GB で CPU に溢れて 0.3 tok/s。4K に絞れば GPU に収まり 25〜30 秒/枚。常に思考するモデルなので思考枠 900 トークンで測定中
- Claude API は環境の鍵が無効で未測定

## まだ

- v2 の採用 step 決定（汎化テスト中: 自転車・後ろ姿・裏地の呼び分け・全身）
- v3: NG 8 枚を差し替え、512 / 1000 step / swap 8 で時短
- 判定ボット（Qwen3-VL 32B の結果次第で Claude API）
- 提出した 160 字の転記

## 次にやること

1. v2 の汎化テストを見て採用 step を決める
2. v3 の素材差し替え（v2 LoRA で生成 → NG 判定 → 登録）と時短設定での学習
3. 判定ボットの組み込み
