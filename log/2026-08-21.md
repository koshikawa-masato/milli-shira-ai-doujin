# 2026-08-21

## やったこと

### キャラ「くろぱんだ」のデザイン確定
- 花札 12 か月の裏地シリーズを生成 → **10月 紅葉に鹿**を正式裏地に採用（★）
- 素材候補 20 枚 → 基準画像「教室で手を振る」（`candidates/20260821-092837-966_0.png`）を**ベスト**に決定
- 方針: **プロンプトは小細工せず、末尾のポーズ文だけ差し替えてパターンを増やす**。仕様を全部書き込んだ v2 プロンプト案は不採用
- 正式プロンプト固定でパターン 15 種 × 2 seed = 30 枚（`patterns`）
- 仕様書: `prompts/kuropanda-design.md`

### LoRA v1 学習
- 素材 29 枚（基準 + patterns 28。横顔 2 枚は破綻で除外）、1800 step / dim 32 / lr 1e-4 / swap 16、185 分
- 同プロンプト比較: 300 で安定、1800 でも過学習なし。LoRA 版は紐が消える（キャプション未記載）
- **トリガー語 `kuropanda` だけではキャラが出ない**（600/900: 耳付きの子、1200: 黒い熊、base: 本物のパンダ）
  - 原因: `kuropanda` が `kuro`+`panda` に分解され、ベースの「パンダ」の知識に負ける
  - v2 案: トリガー語を `panda` を含まない語（例 `krpn`）に変更、キャプションから衣装記述を全部外す、素材を年齢・衣装で 16 枚に絞る、2400 step
- 発見: LoRA 適用時の生成が 14 分/枚 → `gen.sh` に `--blocks_to_swap 12` で 100 秒に

### サークルカット（C109）
- 経緯は `application/circle-cut/README.md`
- 採用: C 案（LoRA 生成「両手の上に浮くミリしら本・びっくり顔」＋黒目を手描き）。カラーとグレースケールの 2 通りを成果物として保存、`~/Downloads/` にもコピー
- 下書き A / B も経過観察として残す

### ギャラリー（Pi5）の改修
- iPhone 最適化: タップでシート、折りたたみ、スワイプ、セーフエリア、閉じるボタン位置
- PC: スクロールバー一本化、系統図のドラッグ移動、⌘+ホイールでズーム、ホバーのプロンプト表示廃止
- チェックポイント名 `-stepNNNNNNNN` 対応、ワーカーの再試行、`;` を含むメタデータの破損修正

## まだ

- Circle.ms でのサークル申込（サークルカットは用意できた）
- LoRA v2（トリガー語の変更）
- 素材の年齢・衣装の統一（v2 で選別）

## 次にやること

1. サークルカットを登録して申込を完了する
2. v2 のトリガー語を決めて学習（素材 16 枚 + 追加 4〜6 枚、2400 step）
3. v2 の短文テスト（`krpn, waving...` だけで出るか）
