# 2026-08-23

## やったこと

### キャラ設定シートの口述（`prompts/kuropanda-character.md`）
- 基本シチュエーション: **学校帰りに天の声（叔父）の家に転がり込んで遊んでいる**。だから本の中の服装は**制服と私服が基本**、黒パンダパーカーは「くろぱんだになる瞬間」の一張羅
- 髪留め: 黒ピン／紅葉モチーフ／白ピン 2 本を編集で試作 → **なし**で確定。理由「高 1 でおしゃれに気が回っていない」「フードを被るので邪魔なものは付けない」→ 性格欄にも反映
- 頭頂部の毛束（アホ毛・寝ぐせ）も試作の上で**なし**で確定
- 制服のディテールを一次情報（写真）から言葉に起こした: 袖口の折り返し、前裾中央の切れ込み、背面の四角い襟と縁のライン、背中のプリンセスライン、後ろ裾のベント

### 着せ替えは「描き直す」から「編集する」へ
- 前日の結論（目を Oracle と完全一致させる）を受け、**Qwen-Image-Edit-2511** を WSL2 に導入（Edit 本体 40.9GB + Qwen2.5-VL 7B 16.6GB）
- `prompts/krea2-scripts/edit.sh` 追加。つまずき: `--negative_prompt` 未指定だと落ちる、`--attn_mode` は `sdpa` でなく `torch`
- **Oracle の服だけ制服に**（顔は画素レベルで一致）→ 刺繍を襟の右側に直した版を**制服バストアップの正**に確定（`edit/20260822-225035-379_0__000.png`、★）
- 1 枚 6〜8 分（4060 Ti は VRAM 不足で 20 ブロック退避＋TE を CPU 実行、11 秒/step）

### 三面図（正面・後ろ・横）の着手と中断
- 参照写真（正面・背面・襟・セット）を切り出して WSL2 `~/krea2/refs/uniform/` に配置（リポジトリ外）
- 参照 4 枚付きの 1216×832 編集は VRAM が溢れて **150 秒/step（1 枚 62 分）** → 中断。クラウド GPU で回し直すことに

### RunPod で RTX PRO 6000 を借りる
- 天の声が RunPod アカウント（$15 クレジット）を用意。**RTX PRO 6000 Blackwell 96GB、$2.11/h**、Network volume 150GB（/workspace、Pod を消しても残る。$0.015/h）
- `prompts/krea2-scripts/setup_runpod.sh`: musubi-tuner + モデル一式（Edit・Turbo・TE×2・VAE ≈ 92GB）を /workspace に取得
- Pod は tailnet 外で Pi5 に届かないので、成果物は **Pod → Mac → ギャラリー** で登録（Mac 側に取り寄せ・登録スクリプト）
- 見込み: 編集 1 枚 6〜8 分 → 20〜30 秒、生成 100 秒 → 10 秒、学習 6.5 時間 → 30〜40 分

### 参考情報
- 「A6000 Pro」の見込み比較（RTX A6000 48GB / RTX PRO 6000 96GB）と、Ollama Cloud・VLM サービス比較は前日ログと会話要約に記載

### 「結構時間かかりますね」の原因と対策（深夜）
- Pod での編集が 1 枚 7〜8 分かかっていた。内訳は **モデル読込（Edit 40GB + TE 16GB を Network volume から）5〜6 分＋デノイズ 2 分**。GPU は読込中ずっと 0%。`edit.sh` を 1 枚ごとに起動していたので、毎回読み直していた
- さらに、完了待ちループの `pgrep -f qwen_image_generate_image` が **自分の ssh コマンド行にマッチ**して「まだ実行中」と誤判定し、30 分の上限まで空回りしていた（GPU は 01:39 から遊んでいた）。判定は tmux セッションの有無で行う
- 対策: `prompts/krea2-scripts/edit_batch.sh` を追加。ジョブ一覧（JSONL）を musubi の `--from_file` 形式（行ごとに `--ci` 参照画像・`--d` seed・`--n` ネガティブ）に変換し、**1 プロセスで連続処理**。読込 1 回＋枚数 × 2 分になる

### RunPod を「使うときだけ動く」形にする（深夜〜午前）
- 天の声「終わった瞬間に止めて、やる時に再スタートさせる処理」→ Serverless（自動スケール 0）と Pod 自動起動停止の 2 案を提示。**学習フェーズなので Pod 案を採用**（Pod $2.09/h vs Serverless $3.49/h）
- `tools/krea2-gallery/app/runpod_worker.py`: Pi5 の中継ワーカー。**Pod モード** = ジョブが来たら REST で Pod 起動 → `pod_boot.sh` で /root を復元 → 参照画像を scp → `edit_batch.sh` を ssh 実行 → 結果を scp 回収してギャラリー登録 → キューが空なら即停止。Serverless モードも同梱
- ギャラリーに「編集」タブ（元画像・参照画像・seed・サイズ・steps・guidance）と、画像詳細の「この画像を編集（RunPod）」を追加。ジョブは種別でワーカーを振り分け（edit → runpod、他 → wsl2）
- `tools/runpod-serverless/`: musubi-tuner を常駐させる handler（読込 150 秒、以後 1 枚 35 秒）。GHCR へ CI でビルド（初回は pip がベース同梱パッケージを外そうとして失敗 → venv 方式に修正）
- **`tools/runpod-menubar/` RunPodBar**: Mac メニューバー常駐アプリ（Swift/AppKit）。● の色で Pod 状態、ホバーで課金・ジョブ、クリックで起動/停止（確認ダイアログ付き）。設定は `~/.config/krea2/runpod.env`
- Pi5 専用の ssh 鍵を作り Pod の `PUBLIC_KEY` に追加 → その env 更新で **コンテナが再作成され、実行中の修正バッチが消えた**（上記教訓）。復元して再実行

### 三面図 v2 の判定（天の声「問題ないと思いますか？」に正直に答えた）
- 正面 seed0: 襟が「深緑の太い帯」になっている、裾がスカートに入っている、タグがポケット内の四角。顔は描き直しなので Oracle と画素は不一致（近いが）
- 横: セーラー襟が無い（丸首 T シャツ状態）。後ろ: 線が 1 本、スカートに「×」、裾が入っている
- 天の声「前と同じで修正、でいけませんか」→ 描き直しでなく、**気になる箇所だけを編集で当てる**方針で修正バッチ（正面・後ろ・横）
- **常駐サーバ（A 案）**: `edit_server.py` を Pod に常駐させ、Pi5 ワーカーはジョブ指示ファイルを置いて待つ方式に。停止猶予 10 分。初回の通し試験（冷間）は Pod 起動〜完了 6 分、2 本目で **scp 書き込み途中のファイルをサーバが拾って落ちる競合**が出た → サーバは「1 秒以上前の空でないファイルだけ拾う・例外で死なない」、ワーカーは「.tmp に送って mv」で解決
- api.runpod.io は `Python-urllib` の User-Agent を 403 で弾く（curl は通る）。独自 UA を付ける
- ギャラリーの自動更新: 20 秒ごとの全再読込だったのを、`/api/version`（履歴と★の更新印）を 10 秒ごとに見て変化時だけ再読込に。タブ復帰時は即時。画像を開いている間は待つ

## 教訓
- **実行中の bash スクリプトを上書きしない**（読み込み途中から壊れる）。差し替えはキューが空いてから
- 参照画像を増やすほど編集の VRAM が跳ね上がる。16GB では 1 枚参照の 1024² が実用上限
- 大きい GPU でも **1 枚ごとにプロセスを起動すると読込が支配的**になる。複数枚は `--from_file` で 1 プロセスにまとめる
- **Pod の env を更新（update-pod）するとコンテナが作り直される**。実行中のジョブは消え、/root（シンボリックリンク・apt で入れた tmux・authorized_keys）は初期化され、ssh の公開ポートも変わる。/workspace（Network volume）だけが残る。→ env 変更は Pod が空いているときに。復元は `pod_boot.sh`。ssh 先のポートは毎回 API から取る
- `pgrep -f <語>` を ssh 越しに使うと自分のコマンド行にマッチする。プロセス待ちは tmux セッションの有無か pid ファイルで
- LoRA を弱めても顔は「似て非なる」になる。完全一致が要るなら編集で画素を保つ

## まだ
- 三面図（正面・後ろ・横）を RunPod で完成させる
- 私服の具体（「結構こだわる」）、一人称・語尾・叔父の呼び方、冬服、持ち物
- ギャラリーに「編集ジョブ」種別を追加（iPhone から編集を投げられるように）
- v3（NG 8 枚の差し替え・512/1000 step の時短）
- Network volume を使わなくなったら削除（$0.015/h）

## 次にやること
1. RunPod のセットアップ完了 → 三面図正面を 1 枚回して速度実測 → 後ろ・横
2. 三面図を設定シートに登録（制服全身の正）
3. 私服の口述 → 編集で着せ替え
