services:
  gallery:
    build: .
    container_name: krea2-gallery
    restart: unless-stopped
    ports:
      - "8020:8020"
    env_file: .env
    environment:
      - TZ=Asia/Tokyo
    volumes:
      - ./data:/data
